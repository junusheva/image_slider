version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run:  echo "Hello, Aijan!"
      - run: echo $(pwd)
      - run: echo $(ls)
      - run: cd src
      - run: yarn run react-scripts test
      - run: echo "Tests passed!"

