version: 2
jobs:
  build:
    docker:
      - image: circleci/node:6.10.3
    steps:
      - checkout
      - run:  echo "Hello, Aijan!"
      - run: echo $(pwd)
      - run: echo $(ls)
      - run: npm install
      - run: npm test
      - run: echo "Tests passed!"

